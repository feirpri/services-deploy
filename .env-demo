# ===== 全局配置（复制为 .env 后修改）=====
# 主域名，所有子服务以 *.${ROOT_DOMAIN} 形式暴露
ROOT_DOMAIN=xxx.cn

# Let's Encrypt 证书申请邮箱
ACME_EMAIL=admin@xxx.cn

# Docker 网络名（caddy 与所有需要被反代的服务共用）
EDGE_NETWORK=edge

# 部署环境：prod | test
DEPLOY_ENV=prod

# 时区
TZ=Asia/Shanghai

# 镜像仓库前缀（自构建镜像使用）
REGISTRY=ghcr.io/yourname
